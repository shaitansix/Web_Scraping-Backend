{ 
  "version": 2, 
  "installCommand": "pip install -r requirements.txt", 
  "devCommand": "uvicorn main:app --reload", 
  "builds": [
    { 
      "src": "*.py", 
      "use": "@vercel/python" 
    }
  ], 
  "routes": [
    { 
      "src": "/(.*)", 
      "dest": "/" 
    }
  ]
}