{ 
  "version": 2, 
  "installCommand": "pip install -r requirements.txt", 
  "devCommand": "py main.py", 
  "builds": [
    { 
      "src": "*.py", 
      "use": "@vercel/python" 
    }
  ], 
  "routes": [
    { 
      "src": "/(.*)", 
      "dest": "/" 
    }
  ]
}