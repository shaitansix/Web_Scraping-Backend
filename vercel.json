{ 
  "version": 2, 
  "installCommand": "pip install -r requirements.txt", 
  "devCommand": "fastapi dev main.py", 
  "builds": [
    { 
      "src": "*.py", 
      "use": "@vercel/python" 
    }
  ], 
  "routes": [
    { 
      "src": "/(.*)", 
      "dest": "/" 
    }
  ]
}